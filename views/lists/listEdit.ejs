<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <title>Edit List</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100">
    <%- include('../partials/nav') %>
        <main class="max-w-md mx-auto p-4 bg-white shadow-md rounded mt-10">
            <h1 class="text-2xl font-bold text-center mb-6">Modifier la liste</h1>
            <form action="/listes/<%= list._id %>?_method=PUT" method="post" class="space-y-4">
                <div>
                    <label for="name" class="block mb-2 font-medium text-gray-700">Nom de la liste</label>
                    <input type="text" id="name" name="listName" value="<%= list.name %>"
                        class="w-full border border-gray-300 rounded p-2" />
                </div>
                <div>
                    <label for="description" class="block mb-2 font-medium text-gray-700">Description</label>
                    <textarea
                      id="description"
                      name="description"
                      rows="4"
                      class="w-full border border-gray-300 rounded p-2"
                    ><%= list.description || '' %></textarea>
                  </div>
                <div class="flex flex-col items-center space-y-4">
                    <button type="submit" class="bg-orange-500 text-white px-4 py-2 rounded hover:bg-orange-600 w-full">
                        Enregistrer
                    </button>

                    <!-- Lien de suppression discret en dessous -->
                    <a href="#"
                        onclick="if(confirm('Êtes-vous sûr de vouloir supprimer cette liste?')) { document.getElementById('delete-form').submit(); } return false;"
                        class="text-red-500 hover:text-red-700 text-sm underline">
                        Supprimer cette liste
                    </a>
                </div>
            </form>

            <!-- Formulaire caché pour la suppression -->
            <form id="delete-form" action="/listes/<%= list._id %>?_method=DELETE" method="post" class="hidden">
            </form>
        </main>
</body>

</html>